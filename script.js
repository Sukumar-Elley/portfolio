const $=s=>document.querySelector(s);const theme=$('#theme'),menu=$('#menu'),nav=document.querySelector('nav');
if(localStorage.getItem('theme')==='dark')document.body.classList.add('dark');
theme.onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem('theme',document.body.classList.contains('dark')?'dark':'light')};
menu.onclick=()=>nav.classList.toggle('open');nav.querySelectorAll('a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
const selected=['finsecure-core','production-rag-system','Data-Leakage-Detection-System','mlops_orchestrator','healthcare-fraud-hub','graph-pulse','ecommerce-retention','Better-Fullstack','agentic-system'];
async function loadProjects(){const grid=$('#projects');try{const r=await fetch('https://api.github.com/users/Sukumar-Elley/repos?per_page=100&sort=updated');if(!r.ok)throw new Error();const repos=await r.json();const map=new Map(repos.map(x=>[x.name,x]));const items=selected.map(n=>map.get(n)).filter(Boolean);grid.innerHTML=items.map((x,i)=>`<a class="project" href="${x.html_url}" target="_blank" rel="noreferrer"><div><span class="project-no">${String(i+1).padStart(2,'0')} / PROJECT</span><h3>${esc(x.name)}</h3><p>${esc(x.description||'Practical software, data and AI engineering.')}</p></div><div class="meta"><span>${esc(x.language||'CODE')}</span><span>★ ${x.stargazers_count}</span><span>VIEW ↗</span></div></a>`).join('')||'<p class="loading">No selected projects found.</p>'}catch(e){grid.innerHTML='<p class="loading">Unable to load GitHub projects. <a href="https://github.com/Sukumar-Elley?tab=repositories">View repositories ↗</a></p>'}}
function esc(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}loadProjects();
